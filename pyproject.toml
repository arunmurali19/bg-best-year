[project]
name = "bg-best-year"
version = "1.0.0"
description = "Board Game Best Year Knockout Tournament"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[project.optional-dependencies]
dev = [
    "pandas>=2.0",
]
