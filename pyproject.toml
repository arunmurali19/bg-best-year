[project]
name = "bg-best-year"
version = "1.0.0"
description = "What's the Good Year? Board Game Knockout Tournament"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "openpyxl>=3.1",
]

[project.optional-dependencies]
dev = [
    "pandas>=2.0",
]
